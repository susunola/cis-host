# ---------------------------------------------------------------------------
# Inventory for the CIS Microsoft Windows Server 2022 Benchmark playbooks.
# Replace the samples below with your own hosts.
# Requires WinRM connectivity: configure HTTPS listener on the target first.
# ---------------------------------------------------------------------------

[win2022]
# win2022-web-01  ansible_host=10.0.1.11
# win2022-web-02  ansible_host=10.0.1.12
# win2022-db-01   ansible_host=10.0.2.21  cis_profile=L2

[win2022:vars]
ansible_connection      = winrm
ansible_winrm_transport = kerberos
ansible_winrm_port      = 5986
ansible_winrm_server_cert_validation = ignore
; ansible_user           = Administrator
; ansible_password       = "{{ vault_winrm_password }}"
; ansible_become_method  = runas
; ansible_become_user    = SYSTEM
